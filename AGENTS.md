# AGENTS.md

## Cursor Cloud specific instructions

This repository (`lighthouse`) is currently an empty/scaffold project with no application code, dependencies, or services. The only file is an empty `README.md` on the `v1` branch.

### Current state

- **No language runtime or package manager** is required yet — there are no dependency manifests (`package.json`, `pyproject.toml`, `go.mod`, etc.).
- **No services** to start, build, or test.
- **No CI/CD** configuration exists.
- **No linting, testing, or build commands** are available.

### When code is added

Once application code is added to this repository, this section should be updated with:

- How to install dependencies (e.g., `npm install`, `pip install -r requirements.txt`).
- How to run the dev server.
- How to run linting and tests.
- Any non-obvious startup caveats or environment requirements.
